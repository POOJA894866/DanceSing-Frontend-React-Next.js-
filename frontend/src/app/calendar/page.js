import CalendarPage from '../../components/pages/CalendarPage';

export const metadata = {
  title: 'Calendar | danceSing',
  description: 'Upcoming danceSing events, workshops, performances, and training sessions.',
};

export default function Calendar() {
  return <CalendarPage />;
}
